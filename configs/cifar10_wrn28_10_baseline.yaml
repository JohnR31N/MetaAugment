competitor:
  name: baseline
data:
  dataset: cifar10
  data_dir: data
  val_size: 1000
  batch_size: 128
  eval_batch_size: 256
  seed: 0
model:
  architecture: wide_resnet
  depth: 28
  width: 10
  dropout_rate: 0.0
augment:
  cutout_size: 16
  epsilon: 0.1
  num_transforms_per_sample: 1
  sampler_update_epochs: 1
  sampler_history_epochs: 50
  translate_const: 10.0
optim:
  epochs: 600
  task_learning_rate: 0.1
  policy_learning_rate: 0.001
  inner_learning_rate: 0.01
  learn_inner_learning_rate: false
  momentum: 0.9
  weight_decay: 0.0005
  label_smoothing: 0.0
  warmup_epochs: 0
  max_grad_norm: 0.0
system:
  workdir: runs/cifar10_wrn28_10_baseline
  seed: 0
  use_pmap: true
  init_distributed: false
  log_every: 50
  eval_every_epochs: 1
  save_every_epochs: 10
  keep_checkpoints: 3
